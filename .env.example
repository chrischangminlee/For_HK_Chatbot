# Rename to .env (local dev) and set your key
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Optional overrides
VITE_GEMINI_MODEL=gemini-2.5-flash
VITE_GEMINI_API_HOST=https://generativelanguage.googleapis.com
VITE_GEMINI_API_VERSION=v1beta

## Optional: increase or remove the output token cap
# If unset, the API default is used. Set to a positive number
# to cap the model output length.
# VITE_GEMINI_MAX_OUTPUT_TOKENS=2048
