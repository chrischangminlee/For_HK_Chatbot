# For GitHub Pages builds, set BASE PATH to "/<repo-name>/"
VITE_BASE_PATH=/your-repo-name/

# WARNING: Embedding API keys in a static site exposes them publicly.
# If you choose to embed anyway, add the key here so the GitHub Action build can inline it.
# Consider referrer restrictions on the key and/or a server proxy.
# VITE_GEMINI_API_KEY=prod_gemini_key

# Optional overrides
# VITE_GEMINI_MODEL=gemini-2.5-flash
# VITE_GEMINI_API_HOST=https://generativelanguage.googleapis.com
# VITE_GEMINI_API_VERSION=v1beta
# Optional: set a different output cap or leave unset to use API default
# VITE_GEMINI_MAX_OUTPUT_TOKENS=2048
